# Q206 · Attrition by department and overtime

Chapters 12 and 14 · beginner · data: `data/generated/hr.csv`

Write:

- `attrition_table(df)`: a DataFrame with departments as rows, `OverTime` values (`No`, `Yes`) as columns
  and the attrition **rate** (mean of `Attrition`) in each cell.
- `riskiest(df)`: the `(Department, OverTime)` pair with the highest rate.

```bash
python practice/exercises/pandas/q206-attrition-by-department-and-overtime/check.py
```
