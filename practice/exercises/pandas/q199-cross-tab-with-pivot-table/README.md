# Q199 · Cross-tab with pivot_table

Chapter 4 · beginner · data: `data/generated/retail.csv`

Write `revenue_by_country_category(df)` returning a DataFrame whose rows are countries, columns are
categories, and cells are total revenue (`Quantity × UnitPrice`), with `0` where a country bought nothing
in a category. Sort rows and columns alphabetically.

```bash
python practice/exercises/pandas/q199-cross-tab-with-pivot-table/check.py
```
