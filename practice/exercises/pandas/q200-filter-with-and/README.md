# Q200 · Filter with & and |

Chapter 4 · beginner · data: `data/generated/retail.csv`

Write `bulk_beans_or_france(df)` returning the rows where `Category` is `Beans` **and** `Quantity` is at
least 10, **or** `Country` is `France`. Keep the original row order and index.

```bash
python practice/exercises/pandas/q200-filter-with-and-or/check.py
```
