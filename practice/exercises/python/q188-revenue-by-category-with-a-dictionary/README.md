# Q188 · Revenue by category with a dictionary

Chapters 3 and 4 · beginner · data: `data/generated/retail.csv`

Without pandas, write `revenue_by_category(path)` that returns a dictionary mapping each `Category` to its
total revenue, where a line's revenue is `Quantity × UnitPrice`. Cancellation lines carry negative
quantities and should simply be added like any other line.

```bash
python practice/exercises/python/q188-revenue-by-category-with-a-dictionary/check.py
```
