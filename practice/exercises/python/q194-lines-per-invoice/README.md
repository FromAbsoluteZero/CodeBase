# Q194 · Lines per invoice

Chapter 3 · beginner · data: `data/generated/retail.csv`

Write:

- `lines_per_invoice(path)`: a dictionary mapping each `InvoiceNo` to the number of rows it has.
- `invoices_with_more_than(path, k)`: how many invoices have more than `k` lines.

```bash
python practice/exercises/python/q194-lines-per-invoice/check.py
```
