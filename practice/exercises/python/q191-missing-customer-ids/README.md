# Q191 · Missing customer IDs

Chapter 11 · beginner · data: `data/generated/retail.csv`

Some order lines have no `CustomerID`. Write `missing_customer_ids(path)` that returns a tuple
`(count, share)`: how many rows have an empty `CustomerID`, and that count divided by the total number of
rows, as a float between 0 and 1.

```bash
python practice/exercises/python/q191-missing-customer-ids/check.py
```
